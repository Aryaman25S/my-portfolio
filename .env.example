# Set at build time so canonical, og:url, and og:image use absolute URLs for link previews.
# No trailing slash. Leave unset for relative image URLs in local dev.
# VITE_SITE_URL=https://www.example.com
